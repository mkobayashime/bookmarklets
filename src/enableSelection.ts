/**
 * @title Enable selection
 * @description
 * Enable text selection with mouse cursor in pages
 * which prohibit doing that
 */

import { enableSelection } from "./utils/enableSelection";

enableSelection();
